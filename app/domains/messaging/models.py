# No new models — the messaging domain uses the existing:
#   Message (table: conversation_messages) from app.models.conversations.conversation
#   MessageDeliveryStatus (table: message_delivery_status) from app.models.conversations.conversation
