# Backward-compatible complete development environment.
# Lightweight production/local startup installs requirements-core.txt only.
-r requirements-core.txt
-r requirements-optional-ai.txt

pytest-asyncio>=0.24.0
